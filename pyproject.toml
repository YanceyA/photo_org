[project]
name = "photoflow"
version = "0.1.0"
description = "Incremental, non-destructive photo library organizer"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
images = ["Pillow", "ImageHash", "pillow-heif"]

[dependency-groups]
dev = ["pytest", "ruff", "Pillow", "ImageHash"]

[project.scripts]
photoflow = "photoflow.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/photoflow"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "exiftool: requires exiftool on PATH (skipped when absent)",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.per-file-ignores]
# verbatim XMP packet template copied from _reference.py; line length is load-bearing
"src/photoflow/xmp.py" = ["E501"]
